-- [[ CLASSIC ISLAND - FINAL OPTIMIZED GLOBAL EDITION (UPDATED REMOTES) ]] --

local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

local Net = ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_net@0.2.0"):WaitForChild("net")

-- ==========================================
-- 1. SETUP UI COMPACT
-- ==========================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ClassicIslandUI"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

-- ICON (TEMA LEGO/MINECRAFT KOTAK)
local IconButton = Instance.new("TextButton")
IconButton.Name = "ClassicIcon"
IconButton.Parent = ScreenGui
IconButton.BackgroundColor3 = Color3.fromRGB(46, 204, 113) -- Warna hijau ala block rumput
IconButton.Position = UDim2.new(0, 15, 0.5, -22)
IconButton.Size = UDim2.new(0, 45, 0, 45)
IconButton.Text = "🧱" -- Icon bata kotak
IconButton.TextColor3 = Color3.fromRGB(255, 255, 255)
IconButton.TextSize = 25
IconButton.Active = true
IconButton.Draggable = true
Instance.new("UICorner", IconButton).CornerRadius = UDim.new(0, 8) -- Sudut lebih tegas (kotak)

-- MENU UTAMA
local MainFrame = Instance.new("Frame")
MainFrame.Name = "ClassicMain"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(245, 255, 245) 
MainFrame.BackgroundTransparency = 0.1 
MainFrame.Position = UDim2.new(0.5, -110, 0.5, -75)
MainFrame.Size = UDim2.new(0, 220, 0, 160) 
MainFrame.Visible = false 
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

-- Header
local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 35)
Header.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 8)

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(0.6, 0, 1, 0)
Title.Position = UDim2.new(0.05, 0, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "Classic Island 🧱" 
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 15
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left

local HideBtn = Instance.new("TextButton", Header)
HideBtn.Position = UDim2.new(0.70, 0, 0.15, 0) 
HideBtn.Size = UDim2.new(0, 25, 0, 25) 
HideBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
HideBtn.BackgroundTransparency = 0.4
HideBtn.Text = "-"
HideBtn.TextColor3 = Color3.fromRGB(39, 174, 96)
HideBtn.TextSize = 18
HideBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", HideBtn).CornerRadius = UDim.new(0, 6)

local CloseBtn = Instance.new("TextButton", Header)
CloseBtn.Position = UDim2.new(0.84, 0, 0.15, 0) 
CloseBtn.Size = UDim2.new(0, 25, 0, 25) 
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.BackgroundTransparency = 0.4
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(39, 174, 96)
CloseBtn.TextSize = 16
CloseBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 6)

-- State Variables
local isWeather = false
local isAutoSell = false
local isFishing = false
local isParty = false
local isNotify = false

-- Reset Function
local BtnWeather, BtnSell, BtnFishing, BtnParty, BtnNotify

local function resetAll()
    isWeather = false; if BtnWeather then BtnWeather.BackgroundTransparency = 0.4 end
    isAutoSell = false; if BtnSell then BtnSell.BackgroundTransparency = 0.4 end
    isFishing = false; if BtnFishing then BtnFishing.BackgroundTransparency = 0.4 end
    isParty = false; if BtnParty then BtnParty.BackgroundTransparency = 0.4 end
    isNotify = false; if BtnNotify then BtnNotify.BackgroundTransparency = 0.4 end
    RunService:UnbindFromRenderStep("Classic_Optimizer")
end

IconButton.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)
HideBtn.MouseButton1Click:Connect(function() MainFrame.Visible = false end)
CloseBtn.MouseButton1Click:Connect(function() resetAll() ScreenGui:Destroy() end)

-- Style Helper
local function styleBtn(b, fSize)
    b.BackgroundColor3 = Color3.fromRGB(39, 174, 96)
    b.BackgroundTransparency = 0.4
    b.TextColor3 = Color3.new(1,1,1)
    b.TextSize = fSize
    b.Font = Enum.Font.GothamBold
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 6)
end

-- ==========================================
-- 2. TOMBOL & LOGIKA (BARIS ATAS)
-- ==========================================
BtnWeather = Instance.new("TextButton", MainFrame)
BtnWeather.Size = UDim2.new(0, 62, 0, 25)
BtnWeather.Position = UDim2.new(0, 10, 0, 45)
BtnWeather.Text = "WEATHER"
styleBtn(BtnWeather, 9)

BtnSell = Instance.new("TextButton", MainFrame)
BtnSell.Size = UDim2.new(0, 62, 0, 25)
BtnSell.Position = UDim2.new(0, 79, 0, 45)
BtnSell.Text = "AUTO SELL"
styleBtn(BtnSell, 8)

local BtnCharm = Instance.new("TextButton", MainFrame)
BtnCharm.Size = UDim2.new(0, 62, 0, 25)
BtnCharm.Position = UDim2.new(0, 148, 0, 45)
BtnCharm.Text = "CHARM"
styleBtn(BtnCharm, 9)

-- Logika Weather (UPDATED REMOTES)
BtnWeather.MouseButton1Click:Connect(function()
    isWeather = not isWeather
    BtnWeather.BackgroundTransparency = isWeather and 0 or 0.4
    if isWeather then
        task.spawn(function()
            local RF_Weather = Net:WaitForChild("RF/28a00ed979fdd6f7b7a2028c590f7ba8d5e259591fd96f6633cd3131451f20db")
            while isWeather do
                pcall(function() RF_Weather:InvokeServer("Cloudy") end)
                task.wait(1)
                
                if not isWeather then break end
                pcall(function() RF_Weather:InvokeServer("Wind") end)
                task.wait(1)
                
                if not isWeather then break end
                pcall(function() RF_Weather:InvokeServer("Storm") end)
                
                -- Jeda 1 Menit (60 Detik)
                for i = 60, 1, -1 do
                    if not isWeather then break end
                    task.wait(1)
                end
            end
        end)
    end
end)

-- Logika Auto Sell (Sistem Toggle)
BtnSell.MouseButton1Click:Connect(function()
    isAutoSell = not isAutoSell
    BtnSell.BackgroundTransparency = isAutoSell and 0 or 0.4
    if isAutoSell then
        task.spawn(function()
            local RF_Sell = Net:WaitForChild("RF/b1d10e8a032ed6a88003228363b8dbab53577efc9d4e9509bb5a8ba124629b0a")
            while isAutoSell do
                pcall(function()
                    RF_Sell:InvokeServer()
                end)
                task.wait(10) -- Loop jual setiap 10 detik agar aman
            end
        end)
    end
end)

-- Logika Charm (UPDATED REMOTES & ARGS)
BtnCharm.MouseButton1Click:Connect(function()
    task.spawn(function()
        BtnCharm.BackgroundTransparency = 0
        local RF_Charm = Net:WaitForChild("RF/b78fc4f408ed8d12f5e98a2541fa03fdc158d4ca0b4fead7398784d2c39618f6")
        for i = 1, 100 do
            pcall(function() RF_Charm:InvokeServer(19) end)
            task.wait(0.1)
        end
        BtnCharm.BackgroundTransparency = 0.4
    end)
end)

-- ==========================================
-- 3. TOMBOL & LOGIKA (BARIS TENGAH)
-- ==========================================
BtnFishing = Instance.new("TextButton", MainFrame)
BtnFishing.Size = UDim2.new(0.32, -4, 0, 28)
BtnFishing.Position = UDim2.new(0.1, 0, 0.52, 0)
BtnFishing.Text = "FISHING"
styleBtn(BtnFishing, 10)

BtnParty = Instance.new("TextButton", MainFrame)
BtnParty.Size = UDim2.new(0.16, -4, 0, 28)
BtnParty.Position = UDim2.new(0.42, 2, 0.52, 0)
BtnParty.Text = "PARTY"
styleBtn(BtnParty, 9)

BtnNotify = Instance.new("TextButton", MainFrame)
BtnNotify.Size = UDim2.new(0.32, -4, 0, 28)
BtnNotify.Position = UDim2.new(0.58, 4, 0.52, 0)
BtnNotify.Text = "NOTIFY"
styleBtn(BtnNotify, 10)

-- Logika Fishing (UPDATED REMOTES)
BtnFishing.MouseButton1Click:Connect(function()
    isFishing = not isFishing
    BtnFishing.BackgroundTransparency = isFishing and 0 or 0.4
    if isFishing then
        task.spawn(function()
            local RF_Charge = Net:WaitForChild("RF/17f9382d6957004bd146ce33ae05b825b851f94802490f558345b9d3be512a82")
            local RF_Start  = Net:WaitForChild("RF/29ef54097b4d1c27e0073ed8a2045b27c3d335ce0dca4bdb4b8180947013c8de")
            local RE_Catch  = Net:WaitForChild("RE/7b70ce8fccce96ad28afe82d35862fa267e5c449b1dcda562412e9bd684c8be9")
            
            while isFishing do
                pcall(function()
                    RF_Charge:InvokeServer()
                    task.wait(math.random(1, 3) / 100) 
                    
                    RF_Start:InvokeServer(-1.233184814453125, 0.34651760989614055, 1777217878.146866)
                    task.wait(math.random(5, 12) / 100) 
                    
                    RE_Catch:FireServer()
                    RE_Catch:FireServer()
                end)
                task.wait(math.random(2, 7) / 100) 
            end
        end)
    end
end)

-- Logika Party (Pengganti Egg - Beli setiap 10 menit)
BtnParty.MouseButton1Click:Connect(function()
    isParty = not isParty
    BtnParty.BackgroundTransparency = isParty and 0 or 0.4
    if isParty then
        task.spawn(function()
            local RE_Party = Net:WaitForChild("RE/896ea2f6dacc6b14cd347714dbca6e2ab0498ae3fa40a0f0c44381270ebf047f")
            while isParty do
                pcall(function()
                    RE_Party:FireServer("Party Wizard", 1, 1)
                end)
                
                -- Hitung mundur 10 menit (600 detik)
                for i = 600, 1, -1 do
                    if not isParty then break end
                    task.wait(1)
                end
            end
        end)
    end
end)

-- Logika Notify (Disable Anim & You Got)
BtnNotify.MouseButton1Click:Connect(function()
    isNotify = not isNotify
    BtnNotify.BackgroundTransparency = isNotify and 0 or 0.4
    if isNotify then
        RunService:BindToRenderStep("Classic_Optimizer", Enum.RenderPriority.Camera.Value + 1, function()
            local char = Player.Character
            if char and char:FindFirstChild("Humanoid") then
                for _, v in pairs(char.Humanoid:GetPlayingAnimationTracks()) do v:Stop() end
            end
            for _, gui in pairs(PlayerGui:GetChildren()) do
                if gui:IsA("ScreenGui") and gui.Name ~= "ClassicIslandUI" then
                    for _, v in pairs(gui:GetDescendants()) do
                        if v:IsA("TextLabel") or v:IsA("ImageLabel") then
                            local txt = v:IsA("TextLabel") and v.Text:lower() or ""
                            if txt:find("you got:") then
                                v.Visible = false
                                if v.Parent:IsA("Frame") then v.Parent.Visible = false end
                            end
                        end
                    end
                end
            end
        end)
    else
        RunService:UnbindFromRenderStep("Classic_Optimizer")
    end
end)

-- ==========================================
-- 4. TOMBOL TELEPORT (BARIS BAWAH)
-- ==========================================
local BtnTeleport = Instance.new("TextButton", MainFrame)
BtnTeleport.Size = UDim2.new(0.8, 0, 0, 28)
BtnTeleport.Position = UDim2.new(0.1, 0, 0.74, 0)
BtnTeleport.Text = "TELEPORT"
styleBtn(BtnTeleport, 12)

BtnTeleport.MouseButton1Click:Connect(function()
    local char = Player.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.CFrame = CFrame.new(1472.36865, 58.0009956, 2856.60547, 0.744470656, 3.18405924e-08, 0.66765517, -3.45702809e-08, 1, -9.14249032e-09, -0.66765517, -1.62747114e-08, 0.744470656)
    end
end)
